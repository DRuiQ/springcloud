server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider    #服务名
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #配置Nacos地址

management:   #暴露服务
  endpoints:
    web:
      exposure:
        include: '*'